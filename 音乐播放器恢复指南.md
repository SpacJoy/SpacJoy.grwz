# 音乐播放器恢复指南

音乐播放器已被临时禁用。如需恢复，请按照以下步骤操作：

## 📋 恢复步骤

### 1. 恢复 HTML 结构

在 `index.html` 文件中，找到被注释的音乐播放器部分：

```html
<!-- 音乐播放器 - 临时禁用 -->
<!--
<div class="music-player" id="musicPlayer">
    ...
</div>
-->
```

**操作：** 删除注释标记 `<!--` 和 `-->`

### 2. 恢复 JavaScript 代码

将备份文件中的代码恢复到 `music-player.js`：

```bash
# 从备份文件恢复
copy music-player-backup-20250623-121335.js music-player.js
```

或者手动复制备份文件中的内容到 `music-player.js`

### 3. 恢复 CSS 样式

在 `main.css` 文件中，找到被注释的音乐播放器样式：

```css
/* 音乐播放器样式 - 临时禁用 */
/*
.music-player {
    ...
}
*/
```

**操作：** 删除注释标记 `/*` 和 `*/`

### 4. 恢复 JavaScript 引用

在 `index.html` 文件中，找到被注释的 JavaScript 引用：

```html
<!-- 音乐播放器 JS - 临时禁用 -->
<!--<script src="./music-player.js"></script>-->
```

**操作：** 删除注释标记，改为：

```html
<script src="./music-player.js"></script>
```

## 📁 备份文件

以下文件包含完整的音乐播放器代码：

-   `music-player-backup-20250623-121335.js` - 最新备份
-   `music-player-backup.js` - 之前的备份

## ✅ 验证恢复

恢复后，刷新页面，您应该能看到：

1. 左下角出现音乐播放器界面
2. 控制台显示音乐播放器初始化信息
3. 播放器功能正常工作

## 📝 注意事项

-   确保所有步骤都完成后再刷新页面
-   如有问题，可以重复上述步骤
-   备份文件会保留，以便将来需要时使用

---

**提示：** 如需再次禁用音乐播放器，可以重新添加注释或运行禁用脚本。
